-#= ff.collection_select :answer, ff.object.survey_question.question.answer_choices, :option_text, :option_text,
-#                        {prompt: false}, {class: 'form-control', multiple: 'multiple', size: 10}
-#
-#= collection_select :observation_answer, :observation_id, ff.object.survey_question.question.answer_choices, :option_text, :option_text,
-#                        {prompt: false}, {class: 'form-control', multiple: 'multiple', size: 10}
-#
-#= select_tag "survey[observations_attributes][#{index}][observation_answers_attributes][multi_answers][]",
-#              options_from_collection_for_select(ff.object.survey_question.question.answer_choices, :option_text, :id, @survey.observations),
-#              {multiple: true, size: 10, class: 'form-control'}

= select_tag "survey[observations_attributes][#{index}][answer_choice_ids][]",
              options_from_collection_for_select(ff.object.survey_question.question.answer_choices, :id, :option_text, @survey.observations),
              {multiple: true, size: 10, class: 'form-control'}