<div class="col-sm-1">
  {{#unless isEditing}}
    <a class="btn btn-xs btn-danger" href="#">
      <span class="glyphicon glyphicon-trash" {{action "deleteQuestion" on="click"}}></span>
    </a>
  {{/unless}}
</div>