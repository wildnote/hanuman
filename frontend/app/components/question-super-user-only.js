import Component from '@ember/component';

export default Component.extend({
  // This maintains the classic component behavior
  // including having a wrapper element and supporting class bindings
  tagName: 'div'
});
