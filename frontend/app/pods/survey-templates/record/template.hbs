<div class="form-horizontal">
  <div class="panel panel-info">
    <div class="panel-title">
      Edit Survey - {{surveyTemplate.name}}
    </div>
    <div class="tab-base tab-stacked-left">
      <ul class="nav nav-tabs">
        <li class="active">
          <a data-toggle="tab" href="/hanuman/admin/survey_templates/{{surveyTemplate.id}}/edit">Survey Layout</a>
        </li>
        <li>
          {{#if hasProjectId}}
            <a href="/hanuman/projects/{{projectId}}/survey_templates/{{surveyTemplate.id}}">Survey Details</a>
          {{else}}
            <a href="/hanuman/survey_templates/{{surveyTemplate.id}}">Survey Details</a>
          {{/if}}
        </li>
      </ul>
      <div class="tab-content">
        <div id="tab-survey-layout" class="tab-pane active">
          {{questions-list surveyTemplate=surveyTemplate updateSortOrderTask=updateSortOrderTask}}
        </div>
        <div id="tab-survey-details" class="tab-pane">
          {{liquid-outlet}}
        </div>
      </div>
    </div>
  </div>
</div>
{{outlet 'questions'}}
